# frozen_string_literal: true

task :generate do
  # Код для генерации лексического анализатора и парсера
  # Например, код для выполнения этой операции
  # puts "Генерация лексического анализатора и парсера..."
  # system("команда для генерации")
end
